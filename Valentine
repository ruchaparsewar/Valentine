<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Will You Be My Valentine?</title>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

<style>
* { box-sizing: border-box; }

body {
  margin: 0;
  height: 100vh;
  background: radial-gradient(circle at top, #ff758c, #ff7eb3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'Poppins', sans-serif;
  overflow: hidden;
}

.container {
  text-align: center;
  color: white;
  max-width: 90%;
  animation: fadeIn 1.5s ease;
}

h1 {
  font-family: 'Playfair Display', serif;
  font-size: 2.6rem;
}

.typing {
  font-size: 1.25rem;
  min-height: 45px;
  margin: 25px 0 45px;
  opacity: 0.95;
}

.buttons {
  position: relative;
  height: 130px;
}

button {
  font-size: 1.1rem;
  padding: 15px 32px;
  border-radius: 35px;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
}

#yesBtn {
  background: #fff;
  color: #ff4b7d;
  box-shadow: 0 15px 35px rgba(0,0,0,0.25);
}

#yesBtn:hover {
  transform: scale(1.12);
}

#noBtn {
  background: transparent;
  color: white;
  border: 2px solid white;
  position: absolute;
}

.celebration {
  display: none;
  animation: fadeIn 1.2s ease forwards;
}

.celebration h2 {
  font-size: 2.3rem;
  margin-bottom: 15px;
}

.celebration p {
  font-size: 1.15rem;
  opacity: 0.95;
  line-height: 1.6;
}

/* Animations */
@keyframes fadeIn {
  from {opacity: 0; transform: translateY(25px);}
  to {opacity: 1; transform: translateY(0);}
}

/* Confetti */
.confetti {
  position: fixed;
  width: 10px;
  height: 10px;
  top: -10px;
  animation: fall 3s linear forwards;
}

@keyframes fall {
  to {
    transform: translateY(110vh) rotate(360deg);
    opacity: 0;
  }
}
</style>
</head>

<body>

<!-- Background Music -->
<audio id="bgMusic" loop>
  <source src="https://cdn.pixabay.com/download/audio/2022/03/15/audio_0b8b8f7c9c.mp3" type="audio/mpeg">
</audio>

<div class="container" id="main">
  <h1>💘 Hey <span id="name"></span> 💘</h1>
  <div class="typing" id="typing"></div>

  <div class="buttons">
    <button id="yesBtn">Yes ❤️</button>
    <button id="noBtn">No 💔</button>
  </div>
</div>

<div class="container celebration" id="celebration">
  <h2>🎉 Congratulations! 🎉</h2>
  <h2>You are officially my Valentine ❤️</h2>
  <p>
    From this moment on,  
    my heart is officially booked by you 💕  
    Thank you for making my life brighter, happier, and full of love.
  </p>
</div>

<script>
/* 🔧 PERSONALIZE NAME HERE */
const partnerName = "SHASHANK";

/* Apply name */
document.getElementById("name").textContent = partnerName;

/* Typing Effect */
const text = `Will you be my Valentine, ${partnerName}?`;
let index = 0;
const typingDiv = document.getElementById("typing");

function typeText() {
  if (index < text.length) {
    typingDiv.innerHTML += text.charAt(index);
    index++;
    setTimeout(typeText, 70);
  }
}
typeText();

/* No Button Escape */
const noBtn = document.getElementById("noBtn");
noBtn.addEventListener("mouseenter", moveNo);
noBtn.addEventListener("click", moveNo);

function moveNo() {
  const x = Math.random() * 260 - 130;
  const y = Math.random() * 140 - 70;
  noBtn.style.transform = `translate(${x}px, ${y}px)`;
}

/* Yes Button Celebration */
const yesBtn = document.getElementById("yesBtn");
const main = document.getElementById("main");
const celebration = document.getElementById("celebration");
const music = document.getElementById("bgMusic");

yesBtn.addEventListener("click", () => {
  main.style.display = "none";
  celebration.style.display = "block";
  music.play();
  launchConfetti();
});

/* Confetti */
function launchConfetti() {
  for (let i = 0; i < 120; i++) {
    const confetti = document.createElement("div");
    confetti.classList.add("confetti");
    confetti.style.left = Math.random() * 100 + "vw";
    confetti.style.backgroundColor =
      ["#fff", "#ff4b7d", "#ffd1dc"][Math.floor(Math.random() * 3)];
    confetti.style.animationDuration = (Math.random() * 2 + 2) + "s";
    document.body.appendChild(confetti);

    setTimeout(() => confetti.remove(), 3000);
  }
}
</script>

</body>
</html>
